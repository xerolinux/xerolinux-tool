#!/bin/bash
yad --title "XeroLinux Common Issue Fixer" --form --columns=2 --width=480 --height=220 --text="Use this tool for troubleshooting.

<b>(*) Those fixes have already been applied to ISO from April 2022 onwards. Apply only if you on older version.</b>" --image=$HOME/xero.png  \
--field="<b>Run Partition Manager</b>":fbtn '/usr/bin/gnome-disks' \
--field="<b>Dolphin Open As Root (*)</b>":fbtn "konsole --noclose -e sh '/usr/local/bin/data/dolphroot'" \
--field="<b>Fix Arch GPG Keys</b>":fbtn "konsole --noclose -e sh '/usr/local/bin/keyfix'" \
--field="<b>Long Shutdown Fix (*)</b>":fbtn "konsole --noclose -e sh '/usr/local/bin/data/shutfix'" \
--field="<b>Add Wayland Support</b>":fbtn "konsole --noclose -e sh '/usr/local/bin/data/xway'" \
--field="<b>Update/Fix Pamac (*)</b>":fbtn "konsole --noclose -e sh '/usr/local/bin/data/rpmc'"  \
--button=Back:1
